#include <iostream>

class verts
{
public:
	float* 	data = NULL;
	int    	count = 0;
	
	//constructors and destructor
	verts();
	verts(int c, float* source);
	~verts();

	//data operation method
	void	set_data(int c, float* source);
	//void	append_data(int c, float* source);
	void	free_data();

	//geometry generate method
	void	verts_cube();
};
